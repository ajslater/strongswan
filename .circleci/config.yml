version: 2.1
jobs:
  alpine-build:
    machine: true
    steps:
      - checkout
      - run: ./build.sh
workflows:
  build:
    jobs:
      - alpine-build:
        pre-steps:
          - run: docker login -u="$DOCKER_USER" -p="$DOCKER_PASS"
        post-steps:
          - run: ./tag-and-push.sh
